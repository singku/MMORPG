1、前后端之间后端之间可能都用到的数据结构体定义在前端的协议目录common.proto里
2、后端之间可能用到的数据结构体定义在后端的common目录里。
3、前后端交互的协议格式采用
   cs_0x0000_name的方式
   前面两个数字(00-FF)和协议所在的文件名相同,协议文件名为0x00.proto
   后面两个数字(00-FF)尽量按顺序使用。
   name表示此协议的具体含义。前端会在编译时把name去掉，后端主要用来方便查看日志.
   前后端的协议交互用命令号标记 0x0000
4、一条前后端的交互协议必须定义两个message cs_0x0000_name sc_0x0000_name 尽量有来必有回，即使请求和响应都是空包
5、错误码定义在errno.txt中(前端协议目录)
6、玩家的一些属性值定义在attribute.xml中(前端协议目录)
7、前后端的协议号段从0x0000-0x7FFF, 服务器之间的协议号段为0x8000-0xFFFF
    switch号段  0x8000 - 0x81FF
    db号段      0x8200 - 0x8AFF
    battle号段  0x8B00 - 0x8BFF
